/* Index-specific styles */
body.index-page {
    padding: 0;
    margin: 0;
    /* Override any potential margin-left from the main CSS */
    margin-left: 0 !important;
    width: 100%;
    overflow-x: hidden;
}

body.index-page .table-of-contents {
    display: none !important;
}

body.index-page main.container {
    margin-left: auto !important;
    margin-right: auto !important;
    max-width: 1200px;
    width: 100%;
    padding: 0 var(--spacing-md);
}

.course-header {
    text-align: center;
    padding: var(--spacing-xl) 0;
}

.course-header h1 {
    margin-bottom: var(--spacing-sm);
    font-size: 2.8rem;
}

.course-description {
    max-width: 800px;
    margin: 0 auto var(--spacing-xl);
    text-align: center;
    color: var(--text-secondary);
    font-size: 1.2rem;
    line-height: 1.6;
    padding: 0 var(--spacing-md);
}

.assignments-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: var(--spacing-lg);
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-md);
}

.assignment-card {
    background: var(--background);
    border-radius: 12px;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    position: relative;
}

.assignment-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
}

.assignment-card img {
    width: 100%;
    height: 180px;
    object-fit: cover;
}

.assignment-card .content {
    padding: var(--spacing-md);
}

.assignment-card h3 {
    margin-bottom: var(--spacing-xs);
    color: var(--text-primary);
}

.assignment-card p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    margin-bottom: var(--spacing-md);
}

.assignment-card .tag {
    display: inline-block;
    background-color: rgba(37, 99, 235, 0.1);
    color: var(--primary-color);
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 0.8rem;
    margin-right: 5px;
    margin-bottom: 5px;
}

.view-btn {
    display: inline-block;
    background-color: var(--primary-color);
    color: white;
    padding: 8px 16px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    transition: background-color 0.2s ease;
}

.view-btn:hover {
    background-color: var(--primary-dark);
}

.semester-indicator {
    position: absolute;
    top: 10px;
    right: 10px;
    background-color: rgba(0, 0, 0, 0.7);
    color: white;
    font-size: 0.8rem;
    padding: 4px 8px;
    border-radius: 4px;
}

/* Center the hero subtitle */
.hero .subtitle {
    text-align: center;
    margin-left: auto;
    margin-right: auto;
}

/* Make the hero section more prominent with the background image */
body.index-page .hero {
    position: relative;
    background: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.2)), url('images/header_background.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    padding: var(--spacing-xxl) 0;
    color: white;
    min-height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
}

body.index-page .hero::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: inherit;
    background-attachment: fixed;
    filter: blur(0);
    transition: filter 0.3s ease;
}

body.index-page .hero .container {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 800px;
}

body.index-page .hero h1,
body.index-page .hero .subtitle {
    color: white;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
}

body.index-page .hero h1 {
    font-size: 3.5rem;
    margin-bottom: var(--spacing-sm);
}

body.index-page .hero .subtitle {
    font-size: 1.3rem;
    font-weight: 400;
    opacity: 0.9;
}

@media (max-width: 768px) {
    body.index-page .hero {
        min-height: 300px;
        padding: var(--spacing-xl) 0;
    }
    
    body.index-page .hero h1 {
        font-size: 2.5rem;
    }
    
    body.index-page .hero .subtitle {
        font-size: 1.1rem;
    }
    
    .assignments-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 480px) {
    body.index-page .hero {
        min-height: 250px;
    }
    
    body.index-page .hero h1 {
        font-size: 2rem;
    }
    
    body.index-page .hero .subtitle {
        font-size: 1rem;
    }
} 

h3 {
    text-align: left;
}